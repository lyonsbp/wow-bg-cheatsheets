Feedback

- smaller anchors when edit route positions (maybe 50%)
- thinner route lines for less clutter on maps with multiple routes (lets make this configurable via a stroke width slider up top)
- the ability to dotted line for alternate paths to a route (just add dotted route typed)
- use the line name in the legend instead of primary route, secondary route, etc. so a leader can call "ramp" and a user can immediately see it's the red line without having to hover each path
- because of all of this, we probably need to revise the route colors to just be red, orange, yellow, green, blue, and purple instead of primary, secondary, etc. I cannot see us needing more than 6 colors, especially if we can have - dotted versions of each color as well.